<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="api.mapper.ApiDao">
    <insert id="insertMember" parameterType="java.lang.String">
        INSERT MEMBER
        (MEM_USERID)
        VALUES(#{memberId})
    </insert>
</mapper>